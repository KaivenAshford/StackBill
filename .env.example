# StackBill Environment Variables
# Copy this file to .env and update values before deployment

# JWT Secret (change this in production!)
JWT_SECRET=change-me-in-production

# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=stackbill
DB_PASSWORD=stackbill_password
DB_NAME=stackbill

# Server
SERVER_PORT=8080
