server:
  port: 8080
  mode: debug  # debug, release, test

database:
  host: localhost
  port: 5432
  user: stackbill
  password: stackbill_password
  dbname: stackbill
  sslmode: disable

jwt:
  secret: ${JWT_SECRET}
  expire_hours: 72

log:
  level: info  # debug, info, warn, error
  format: json  # json, text
