server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: stackbill
  password: stackbill_password
  dbname: stackbill
  sslmode: disable

jwt:
  secret: dev-secret-key-change-in-production
  expire_hours: 72

log:
  level: debug
  format: text
